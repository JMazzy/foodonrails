<!DOCTYPE html>
<html>
<head>
  <title>Food On Rails</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>

  <link rel="stylesheet" href="https://code.getmdl.io/1.1.3/material.blue-light_blue.min.css" />

  <script src="//maps.google.com/maps/api/js" type="text/javascript"></script>
  <script src="//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js" type="text/javascript"></script>

</head>
  <body>

    <!-- Always shows a header, even in smaller screens. -->
    <div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
      <header class="mdl-layout__header">
        <div class="mdl-layout__header-row">
          <!-- Title -->
          <span class="mdl-layout-title">Food On Rails</span>
          <!-- Add spacer, to align navigation to the right -->
          <div class="mdl-layout-spacer"></div>
          <!-- Navigation. We hide it in small screens. -->
          <nav class="mdl-navigation mdl-layout--large-screen-only">
            <a class="mdl-navigation__link" href="">Map</a>
            <% if signed_in_user? %>
              <%= link_to current_user.username, current_user, class: "mdl-navigation__link" %>
              <%= link_to "Logout", logout_path, :method => :delete, class: "mdl-navigation__link" %>
            <% else %>
              <%= link_to "Login", login_path, class: "mdl-navigation__link" %>
            <% end %>

            <%= link_to "Sign Up", new_user_path, class: "mdl-navigation__link" %>
          </nav>
        </div>
      </header>

      <main class="mdl-layout__content">
        <div class="page-content">

          <div class="mdl-grid">
            <div class="mdl-cell mdl-cell--9-col">

              <%= yield %>

            </div>

            <div class="mdl-cell mdl-cell--3-col">



            </div>
          </div>

        </div>
      </main>
    </div>

  </body>
</html>
